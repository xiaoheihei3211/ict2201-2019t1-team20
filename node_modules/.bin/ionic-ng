#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ionic/ng-toolkit/bin/ionic-ng" "$@"
  ret=$?
else 
  node  "$basedir/../@ionic/ng-toolkit/bin/ionic-ng" "$@"
  ret=$?
fi
exit $ret
